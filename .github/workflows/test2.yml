name: tester2
on: workflow-dispatch
jobs:
  tester:
    runs-on: ubuntu-latest
    steps:
      - name: check out repostitory
        uses: actions/checkout@v4
      - name: shell
        shell: bash
        run: |
          date
          echo $RANDOM
          cat bin/tester
      - run: "🍏 This jobs status is ${{ job.status }}."
