name: tester
'on': push
jobs:
  tester:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: check out repostitory
        uses: actions/checkout@v4
      - name: shell
        shell: bash
        env:
          PASS: ${{ secrets.WACHTWOORD }}
          GP: ${{ secrets.GMAIL_PASSWORD }}
          SS: ${{ secrets.SECRET }}
        run: |
          echo git-ref ${{ github.ref_name }}
          echo ${{ github.workspace }}
          ls -R ${{ github.workspace }}
          echo home $HOME
          echo home3 ${{ vars.HOME }}
          echo user ${{ vars.USERNAME }}
          bin/verklap ">> $PASS"
          bin/verklap "># ${{ secrets.SECRET }}"
          bin/verklap "># $GP"
          cat bin/tester
      - name: Send mail
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: rbg.sdu@gmail.com
          password: $GP
          subject: groot nieuws
          to: r.grosmann@sdu.nl
          from: rbg.sdu@gmail.com
          body: "dit is echt goed nieuw"
          priority: low
      - run: echo "🍏 This jobs status is ${{ job.status }}."
