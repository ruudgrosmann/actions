name: tester
'on': push
jobs:
  tester:
    runs-on: ubuntu-latest
    steps:
      - name: shell
        shell: bash
        run: |
          pwd
          ls -l
          cat bin/tester
          echo dit is een test
