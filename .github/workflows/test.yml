name: tester
on: [push]
jobs:
  my-job: tester
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Write a multi-line message
        run: |
            cat bin/tester
            echo dit is een test
