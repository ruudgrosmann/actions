name: tester
'on': push
jobs:
  tester:
    runs-on: ubuntu-latest
    steps:
      - name: shell
        shell: bash
        run: |
          pwd
          ls -lR ../../../..
          ls ${{ github.workspace }}
          echo dit is een test
