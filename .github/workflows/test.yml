name: tester
on: push
jobs:
  tester:
    runs-on: ubuntu-latest
    environment: dev
    env:
      POSTMARK_TOKEN: ${{ secrets.POSTMARK_API_TOKEN }}
    steps:
      - name: check out repostitory
        uses: actions/checkout@v4
        with:
          ref: main
      - name: controle of package.json is gewijzigd
        if: ${{  git diff --name-only HEAD HEAD~2 | grep package.json }}
        shell: bash
        run: |
          echo package.json is gewijzigd
          echo extra build nodig
      - name: acties
        shell: bash
        run: |
          echo "= 03 ========"
          echo home $HOME
          echo home3 ${{ vars.HOME }}
          bin/verklap "># ${{ secrets.SECRET }}"
          cat bin/tester
      - name: extra test
        shell: bash
        run: |
          echo "=  extra test ========"
        run: |
          echo "=  extra test2 ========"
      - run: echo "🍏 The status is ${{job.status}}."

